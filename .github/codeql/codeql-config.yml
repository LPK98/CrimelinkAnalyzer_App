name: "CodeQL config"

# Scan only your source, skip generated / native / vendor folders
paths-ignore:
  - node_modules
  - dist
  - build
  - coverage
  - .expo
  - .next
  - android
  - ios
  - web-build
  - expo-env.d.ts
