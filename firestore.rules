rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporarily allowing all access for development to fix the permission issues
      allow read, write: if true;
    }
  }
}
